'use strict';

import * as types from '../constants/actionTypes';

const initState = {
  
};

export default function task(state = initState, action){
  switch (action.type) {
      default:
          return state;
  }
}
